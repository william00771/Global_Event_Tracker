import './CreateEvent.css'

type Props = {
    className: string
}

export const CreateEvent = ({ className }: Props) => {
    return(
        <section className={className}>
            <h1>CreateEvent</h1>
        </section>
    )
}